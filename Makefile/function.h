#ifndef FUNCTIONS_H  //ตรวจสอบว่ามาโคร FUNCTIONS_H ถูกกำหนดไว้แล้วหรือไม่ 
#define FUNCTIONS_H //กำหนดมาโคร FUNCTIONS_H ขึ้นมาทันทีหลังจากที่ตรวจสอบแล้วว่ายังไม่ได้ถูกกำหนดไว้

void print_hello();  //บอกคอมไพเลอร์ว่ามีฟังก์ชันชื่อ print_hello อยู่ในโปรแกรม

#endif //เป็นการบอกจุดสิ้นสุดของโค้ดที่จะถูกคอมไพล์ก็ต่อเมื่อมาโคร FUNCTIONS_H ยังไม่ถูกกำหนดไว้ก่อนหน้านี้
